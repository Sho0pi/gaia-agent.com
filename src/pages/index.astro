---
import Layout from '../layouts/Layout.astro';
import { Icon } from 'astro-icon/components';
---
<Layout
  title="Gaia: agents that build agents"
  description="Gaia is an open-source AI agent that hands work to specialist souls, and builds new ones when nothing fits."
  active="home"
>

<style is:global>
  /* === Glitch swap — linear timing (not steps) === */
  .glitch-wrap { position:relative; display:inline-block; line-height:1; padding-right:.35em; font-size:1.2em; vertical-align:baseline; }
  .glitch-wrap .gw-en, .glitch-wrap .gw-gr { color:var(--ink); display:inline-block; }
  .glitch-wrap .gw-en { font-family:'Fraunces',serif; font-weight:700; font-style:italic; font-variation-settings:"opsz" 144,"SOFT" 100; letter-spacing:-0.04em; }
  .glitch-wrap .gw-gr { font-family:'Cinzel',serif; font-weight:900; position:absolute; left:0; top:0; opacity:0; letter-spacing:.02em; }
  .glitch-wrap.glitching .gw-en { animation: glitch-out 10s linear infinite; }
  .glitch-wrap.glitching .gw-gr { animation: glitch-in  10s linear infinite; }
  @keyframes glitch-out {
    0%,73%  { opacity:1; transform:translate(0,0); clip-path:inset(0 0 0 0); }
    74%     { opacity:1; transform:translate(2px,-1px); clip-path:inset(0 0 60% 0); }
    75%     { opacity:1; transform:translate(-2px,1px); clip-path:inset(40% 0 0 0); }
    76%     { opacity:.4; }
    77%,96% { opacity:0; }
    97%     { opacity:.4; clip-path:inset(0 0 50% 0); }
    98%     { opacity:.7; clip-path:inset(50% 0 0 0); }
    99%,100%{ opacity:1; transform:translate(0,0); clip-path:inset(0 0 0 0); }
  }
  @keyframes glitch-in {
    0%,76%  { opacity:0; }
    77%     { opacity:.6; clip-path:inset(0 0 50% 0); }
    78%     { opacity:.8; clip-path:inset(50% 0 0 0); }
    79%,96% { opacity:1; clip-path:inset(0 0 0 0); }
    97%     { opacity:.7; clip-path:inset(0 0 60% 0); }
    98%     { opacity:.4; clip-path:inset(40% 0 0 0); }
    99%,100%{ opacity:0; }
  }

  /* === Verb cycle === */
  .verb-cycle { display:inline-block; position:relative; }
  .verb-cycle .verb {
    display:inline-block; color:var(--moss);
    font-family:'Fraunces',serif; font-weight:600; font-style:italic;
    transition: opacity .4s ease, transform .4s ease, filter .4s ease;
  }
  .verb-cycle .verb.out { opacity:0; transform:translateY(-8px); filter:blur(3px); }
  .verb-cycle .verb.in  { opacity:0; transform:translateY(10px); filter:blur(3px); }

  /* === Forge demo — user request → Gaia creates best-fit soul === */
  .forge-lab {
    display:grid; grid-template-columns:1fr; gap:1rem;
    max-width:820px; margin:2.25rem auto 0; text-align:left;
  }
  @media (min-width:820px) {
    .forge-lab { grid-template-columns:minmax(0,1.05fr) 88px minmax(0,.95fr); align-items:stretch; }
  }
  .forge-panel {
    border:1px solid var(--rule); background:var(--surface);
    min-height:278px; padding:1.1rem; position:relative; overflow:hidden;
  }
  .forge-panel::after {
    content:""; position:absolute; inset:auto 1.1rem 0; height:1px;
    background:linear-gradient(90deg, transparent, var(--moss), transparent);
    opacity:.45;
  }
  .forge-kicker {
    font-family:'JetBrains Mono',monospace; font-size:.68rem;
    text-transform:uppercase; letter-spacing:.14em; color:var(--kelp);
  }
  .request-card {
    margin-top:1rem; height:146px; border:1px solid color-mix(in srgb, var(--rule) 78%, transparent);
    background:var(--bg); padding:1rem; display:flex; flex-direction:column; justify-content:space-between;
    overflow:hidden;
    transition:opacity .28s ease, transform .28s ease, border-color .28s ease;
    animation:forge-card-breathe 3.4s ease-in-out infinite;
  }
  .request-card.is-switching { opacity:.28; transform:translateY(8px); }
  .request-text {
    font-family:'Fraunces',serif; font-size:clamp(1.05rem,2.8vw,1.45rem);
    line-height:1.28; color:var(--ink);
  }
  .request-meta { margin-top:1rem; font-family:'JetBrains Mono',monospace; font-size:.72rem; color:var(--ink-soft); }
  .forge-bridge {
    display:flex; align-items:center; justify-content:center; min-height:72px;
    color:var(--moss); font-family:'Cinzel',serif; letter-spacing:.28em; font-size:.68rem;
  }
  .forge-pulse {
    width:64px; height:64px; border:1px solid var(--moss); border-radius:50%;
    display:flex; align-items:center; justify-content:center; position:relative;
    background:color-mix(in srgb, var(--surface) 82%, transparent);
    box-shadow:0 0 24px color-mix(in srgb, var(--moss) 22%, transparent);
  }
  .forge-pulse::before, .forge-pulse::after {
    content:""; position:absolute; inset:-8px; border:1px solid var(--moss); border-radius:50%;
    opacity:.45; animation:forge-ring 2.2s ease-out infinite;
  }
  .forge-pulse::after { animation-delay:.75s; }
  .forge-pulse img {
    width:52px; height:52px; object-fit:contain; position:relative; z-index:1;
    filter:drop-shadow(0 0 8px color-mix(in srgb, var(--moss) 46%, transparent));
    animation:forge-icon-pulse 2.2s ease-in-out infinite;
  }
  @keyframes forge-ring {
    0% { transform:scale(.72); opacity:.6; }
    100% { transform:scale(1.45); opacity:0; }
  }
  @keyframes forge-icon-pulse {
    0%, 100% { transform:scale(.94); opacity:.86; }
    50% { transform:scale(1.06); opacity:1; }
  }
  .agent-card {
    margin-top:1rem; border:1px solid color-mix(in srgb, var(--moss) 72%, var(--rule));
    background:color-mix(in srgb, var(--moss) 6%, var(--surface)); padding:1rem;
    height:218px; overflow:hidden; transition:opacity .28s ease, transform .28s ease;
    animation:forge-card-breathe 3.4s ease-in-out infinite .25s;
  }
  .agent-card.is-switching { opacity:.3; transform:translateY(8px); }
  .agent-role {
    font-family:'Fraunces',serif; color:var(--moss); font-size:clamp(1.2rem,3vw,1.7rem);
    font-weight:600; font-style:italic; line-height:1.12;
  }
  .agent-fit { margin-top:.55rem; color:var(--ink-soft); font-size:.86rem; line-height:1.55; }
  .agent-tools {
    margin-top:.85rem; display:flex; flex-wrap:wrap; gap:.45rem;
  }
  .agent-tools span {
    border:1px solid color-mix(in srgb, var(--rule) 85%, transparent);
    padding:.28rem .5rem; font-family:'JetBrains Mono',monospace; font-size:.68rem; color:var(--ink-soft);
    background:color-mix(in srgb, var(--bg) 72%, transparent);
  }
  .forge-steps {
    margin-top:1rem; display:grid; grid-template-columns:repeat(3,1fr); gap:.45rem;
    font-family:'JetBrains Mono',monospace; font-size:.66rem; color:var(--ink-soft);
  }
  .forge-step {
    border-top:1px solid var(--rule); padding-top:.5rem; opacity:.55;
    transition:opacity .2s ease, color .2s ease, border-color .2s ease;
  }
  .forge-step.active { opacity:1; color:var(--moss); border-color:var(--moss); }
  @keyframes forge-card-breathe {
    0%, 100% { border-color:color-mix(in srgb, var(--rule) 78%, transparent); }
    50% { border-color:color-mix(in srgb, var(--moss) 78%, var(--rule)); }
  }
  @media (prefers-reduced-motion: reduce) {
    .forge-pulse::before, .forge-pulse::after { animation:none; opacity:.2; }
    .forge-pulse img { animation:none; }
    .request-card, .agent-card { animation:none; }
    .request-card, .agent-card, .forge-step { transition:none; }
  }

  .card { background:var(--surface); border:1px solid var(--rule); border-radius:0; transition: transform .35s cubic-bezier(.2,.8,.2,1), border-color .25s; }
  .card:hover { transform:translateY(-2px); border-color:var(--moss); }

  .drop { font-family:'Cinzel',serif; font-weight:900; color:var(--moss); opacity:.18; line-height:.85; user-select:none; }

  .hero-compact { padding-top:3.75rem; padding-bottom:3.25rem; }
  .hero-compact h1 { max-width:11ch; }
  @media (max-width:767px) {
    .hero-compact { padding-top:2.7rem; padding-bottom:2.5rem; }
  }

  /* Terminal shell block — parchment in light theme, phosphor green in dark */
  .shell-wrap { position:relative; border:1px solid var(--rule); box-shadow:0 24px 60px -32px color-mix(in srgb, var(--ink) 45%, transparent); }
  .shell-bar { position:relative; display:flex; align-items:center; padding:.6rem 1rem; background:var(--bg-deep); border-bottom:1px solid var(--rule); }
  .shell-dots { display:flex; gap:.5rem; }
  .shell-dot { width:11px; height:11px; border-radius:50%; box-shadow:inset 0 1px 1px rgba(255,255,255,.25), inset 0 -1px 1px rgba(0,0,0,.25); }
  .shell-dot.r { background:#ff5f57; } .shell-dot.y { background:#febc2e; } .shell-dot.g { background:#28c840; }
  .shell-title { position:absolute; left:50%; transform:translateX(-50%); font-family:'JetBrains Mono',monospace; font-size:.68rem; letter-spacing:.1em; color:color-mix(in srgb, var(--ink) 45%, transparent); user-select:none; }
  .copy-btn {
    margin-left:auto; font-family:'JetBrains Mono',monospace; font-size:.68rem;
    color:var(--ink-soft); background:color-mix(in srgb, var(--ink) 5%, transparent);
    border:1px solid var(--rule); padding:.25rem .6rem;
    cursor:pointer; border-radius:0; transition:all .15s;
  }
  .copy-btn:hover { background:color-mix(in srgb, var(--ink) 12%, transparent); color:var(--ink); border-color:var(--ink-soft); }
  .copy-btn.copied { color:var(--moss); border-color:var(--moss); background:color-mix(in srgb, var(--moss) 10%, transparent); }
  .vellum { font-family:'JetBrains Mono',monospace; font-size:.88rem; line-height:1.7; background:var(--surface); padding:1.5rem 1.75rem; color:var(--ink-soft); }
  .vellum .p { color:var(--moss); font-weight:600; } .vellum .c { color:var(--ink); } .vellum .ok { color:var(--moss); }
  .vellum .dim { color:color-mix(in srgb, var(--ink-soft) 65%, transparent); }
  .cursor { display:inline-block; width:8px; height:1em; background:var(--moss); vertical-align:text-bottom; margin-left:3px; animation:cursor-blink 1.1s steps(2,start) infinite; }
  @keyframes cursor-blink { 50% { opacity:0; } }
  @media (prefers-reduced-motion: reduce) { .cursor { animation:none; } }

  [data-theme="dark"] .shell-wrap { box-shadow:0 24px 60px -28px rgba(0,0,0,.85); }
  [data-theme="dark"] .shell-bar { background:#0A1610; border-bottom:1px solid rgba(255,255,255,.06); }
  [data-theme="dark"] .shell-title { color:rgba(226,250,232,.35); }
  [data-theme="dark"] .copy-btn { color:rgba(226,250,232,.5); background:rgba(255,255,255,.06); border-color:rgba(255,255,255,.12); }
  [data-theme="dark"] .copy-btn:hover { background:rgba(255,255,255,.12); color:#fff; border-color:rgba(255,255,255,.25); }
  [data-theme="dark"] .copy-btn.copied { color:var(--glow); border-color:var(--glow); background:rgba(34,240,168,.08); }
  [data-theme="dark"] .vellum { background:#0d1a10; color:#8BAF94; }
  [data-theme="dark"] .vellum .p, [data-theme="dark"] .vellum .ok { color:#4ADE80; }
  [data-theme="dark"] .vellum .c { color:#E2FAE8; }
  [data-theme="dark"] .vellum .dim { color:#5a7a62; }
  [data-theme="dark"] .cursor { background:#4ADE80; }

  .live { width:7px; height:7px; border-radius:9999px; background:var(--glow); box-shadow:0 0 0 0 var(--glow); animation: live-pulse 2.4s ease-out infinite; }
  @keyframes live-pulse {
    0%   { box-shadow:0 0 0 0 color-mix(in srgb, var(--glow) 60%, transparent); }
    100% { box-shadow:0 0 0 10px transparent; }
  }

  .ico { stroke:var(--ink); stroke-width:1.25; fill:none; stroke-linecap:round; stroke-linejoin:round; }
  .card:hover .ico { stroke:var(--moss); }

  .smith { background:var(--surface); border:1px solid var(--rule); padding:2.25rem 2rem; position:relative; }
  .smith-grid { display:grid; grid-template-columns:1fr; gap:1.5rem; font-family:'JetBrains Mono',monospace; font-size:.78rem; color:var(--ink-soft); }
  .node { display:flex; align-items:center; gap:.85rem; padding:.7rem 1rem; border:1px solid var(--rule); background:var(--bg); }
  .node .label { color:var(--ink); }
  .node .role { color:var(--moss); font-style:italic; font-family:'Fraunces',serif; font-size:.92rem; font-weight:500; }
  .arrow { text-align:center; color:var(--moss); font-family:'Cinzel',serif; font-size:.8rem; letter-spacing:.3em; padding:.25rem 0; }
  .arrow::before { content:"│"; display:block; opacity:.3; margin-bottom:.25rem; }
  .arrow::after  { content:"│"; display:block; opacity:.3; margin-top:.25rem; }

  blockquote.dictum { border-left:2px solid var(--moss); padding:.4rem 0 .4rem 1.25rem; font-family:'Fraunces',serif; font-style:italic; font-weight:400; font-size:1.05rem; color:var(--ink-soft); line-height:1.5; }

  /* Star count badge */
  .star-badge {
    display:inline-flex; align-items:center; gap:.5rem;
    border:1px solid var(--rule); padding:.35rem .85rem;
    font-family:'JetBrains Mono',monospace; font-size:.78rem;
    color:var(--ink-soft); background:var(--surface);
    transition: border-color .2s, color .2s;
    text-decoration:none;
  }
  .star-badge:hover { border-color:var(--moss); color:var(--ink); }
  .star-badge .star-ico { color:var(--gold); }
  .star-badge .star-num { color:var(--ink); font-weight:600; }

  /* Stats bar */
  .stat-block { transition: background .2s; }
  .stat-block:hover { background: color-mix(in srgb, var(--moss) 5%, transparent); }

  /* How it works */
  .how-num {
    display:inline-block; font-family:'Cinzel',serif; font-weight:700;
    font-size:.75rem; letter-spacing:.2em;
    border:1px solid var(--rule); padding:.3rem .6rem; color:var(--ink-soft);
  }
  .how-active { background: color-mix(in srgb, var(--moss) 5%, transparent); }
  .how-active .how-num { color:var(--moss); border-color:var(--moss); }

  /* FAQ */
  .faq-item { border-bottom:1px solid var(--rule); }
  .faq-item:first-child { border-top:1px solid var(--rule); }
  .faq-q {
    width:100%; display:flex; justify-content:space-between; align-items:center;
    cursor:pointer; padding:1.1rem 0; list-style:none;
    font-size:.95rem; font-weight:500; color:var(--ink); text-align:left; gap:1rem;
  }
  .faq-q::-webkit-details-marker { display:none; }
  .faq-icon { font-size:1.2rem; color:var(--moss); flex-shrink:0; transition:transform .2s; }
  .faq-item[open] .faq-icon { transform:rotate(45deg); }
  .faq-a {
    padding:0 0 1.1rem; color:var(--ink-soft);
    font-size:.9rem; line-height:1.7;
  }

  /* Voices — testimonials + tweet wall */
  .quote-card { background:var(--surface); border:1px solid var(--rule); padding:1.6rem 1.5rem; display:flex; flex-direction:column; transition:border-color .25s, transform .35s cubic-bezier(.2,.8,.2,1); }
  .quote-card:hover { border-color:var(--moss); transform:translateY(-2px); }
  .quote-card .mark { font-family:'Cinzel',serif; font-weight:900; font-size:1.6rem; line-height:.6; color:var(--moss); opacity:.5; }
  .quote-card blockquote { font-family:'Fraunces',serif; font-size:1rem; line-height:1.55; color:var(--ink); margin:.9rem 0 1.25rem; }
  .quote-card .who { margin-top:auto; display:flex; align-items:center; gap:.7rem; }
  .quote-card .avatar { width:34px; height:34px; border-radius:50%; background:color-mix(in srgb, var(--moss) 18%, var(--surface)); border:1px solid var(--rule); display:flex; align-items:center; justify-content:center; font-family:'Cinzel',serif; font-weight:700; font-size:.8rem; color:var(--moss); flex-shrink:0; }
  .quote-card .name { font-size:.82rem; font-weight:600; color:var(--ink); }
  .quote-card .handle { font-family:'JetBrains Mono',monospace; font-size:.72rem; color:var(--ink-soft); }
  /* Tweet wall (dormant until wired to X API/embed) */
  #tweetWall:empty { display:none; }
  .tweet-empty { border:1px dashed var(--rule); padding:1.5rem; text-align:center; color:var(--ink-soft); font-size:.84rem; }
</style>

<main class="hero-compact relative z-10 max-w-5xl mx-auto px-6">
  <div class="grid md:grid-cols-12 gap-10 items-center">
    <div class="md:col-span-7">
      <p class="eyebrow">Γαῖα · agents that build agents</p>
      <h1 class="display mt-6 text-[clamp(3rem,7vw,5.6rem)] leading-[1.02] tracking-tight">
        Why do the task<br/>
        when you can <span class="verb-cycle"><span id="verb" class="verb">forge</span></span><br/>
        someone to do it?
      </h1>
      <div class="mt-7 flex items-start gap-6">
        <span class="drop text-[6.5rem] hidden sm:block" aria-hidden="true">Γ</span>
        <p class="muted max-w-md text-[15px] leading-relaxed pt-2">
          <span class="glitch-wrap glitching"><span class="gw-en">Gaia</span><span class="gw-gr" aria-hidden="true">Γαῖα</span></span>
          is a new kind of AI agent. Instead of doing every task itself, it builds the agents that do. It picks the right one for the job, makes a new one when nothing fits, and keeps making them better as they go.
        </p>
      </div>
      <div class="mt-7 flex flex-wrap items-center gap-3">
        <a id="starBadge" href="https://github.com/sho0pi/gaia" target="_blank" class="btn btn-primary">
          <span style="color:var(--gold);">★</span>
          Star on GitHub
          <span class="star-num" id="starCount" style="color:inherit;opacity:.7;"></span>
        </a>
        <a href="#install" class="btn btn-ghost">Get started ↓</a>
      </div>
      <div class="mt-4 flex items-center gap-2 text-xs muted">
        <span class="live"></span> open source · MIT · runs on macOS, Linux, Windows
      </div>
    </div>
    <div class="md:col-span-5">
      <figure class="relative">
        <img src="/hero.jpg" alt="" class="w-full" style="border:1px solid var(--rule);" />
        <figcaption class="mono text-[10px] tracking-widest uppercase mt-3 muted">Fig. I · gaia, mother of agents</figcaption>
      </figure>
    </div>
  </div>
</main>

<hr class="hr-script max-w-5xl mx-auto" />

<section id="install" class="relative z-10 max-w-5xl mx-auto px-6 py-12 md:py-14">
  <div class="grid md:grid-cols-12 gap-8 items-center">
    <div class="md:col-span-4">
      <p class="eyebrow">I · Begin</p>
      <h2 class="display text-3xl md:text-4xl mt-4 leading-tight tracking-tight">One line.<br/>Any machine.</h2>
      <p class="muted text-[14px] mt-4 leading-relaxed">macOS, Linux, Windows. Bring your own keys, or self-host the whole thing.</p>
    </div>
    <div class="md:col-span-8">
      <div class="shell-wrap">
        <div class="shell-bar">
          <span class="shell-dots" aria-hidden="true">
            <span class="shell-dot r"></span>
            <span class="shell-dot y"></span>
            <span class="shell-dot g"></span>
          </span>
          <span class="shell-title">gaia@earth ~ bash</span>
          <button class="copy-btn" id="copyBtn" onclick="copyInstall()">copy</button>
        </div>
        <div class="vellum">
          <div><span class="p">$</span> <span class="c" id="installCmd">curl -fsSL https://gaia-agent.com/install.sh | bash</span></div>
          <div class="mt-3 dim">→ planting roots ......... <span class="ok">ok</span></div>
          <div class="dim">→ pairing channels ....... <span class="c">whatsapp · telegram · discord</span></div>
          <div class="dim">→ <span class="ok">gaia ready.</span> say hello to your agent.<span class="cursor" aria-hidden="true"></span></div>
        </div>
      </div>
    </div>
  </div>
</section>

<hr class="hr-script max-w-5xl mx-auto" />

<section class="relative z-10 max-w-5xl mx-auto px-6 pt-16 pb-20 text-center">
  <p class="eyebrow mb-5">the gaia way</p>
  <h2 class="display text-3xl md:text-[2.6rem] leading-[1.1] tracking-tight max-w-2xl mx-auto">
    Don't do the task.<br/>
    <span class="italic" style="color:var(--moss);">Spin up someone who will.</span>
  </h2>

  <div class="forge-lab" aria-live="polite">
    <div class="forge-panel">
      <div class="forge-kicker">user asks</div>
      <div id="requestCard" class="request-card">
        <div id="requestText" class="request-text">Build me a launch page and make it feel alive.</div>
        <div id="requestMeta" class="request-meta">needs: copy · design · frontend</div>
      </div>
      <div class="forge-steps" aria-hidden="true">
        <div class="forge-step active" data-forge-step="read">read task</div>
        <div class="forge-step" data-forge-step="shape">shape soul</div>
        <div class="forge-step" data-forge-step="assign">assign tools</div>
      </div>
    </div>
    <div class="forge-bridge" aria-hidden="true">
      <div class="forge-pulse"><img src="/gaia-icon.png" alt="" loading="lazy" /></div>
    </div>
    <div class="forge-panel">
      <div class="forge-kicker">gaia forges</div>
      <div id="agentCard" class="agent-card">
        <div id="agentRole" class="agent-role">soul · product web designer</div>
        <p id="agentFit" class="agent-fit">Best fit for turning rough intent into a polished page, copy, layout, and deployable frontend.</p>
        <div id="agentTools" class="agent-tools">
          <span>Astro</span><span>Tailwind</span><span>copy pass</span>
        </div>
      </div>
    </div>
  </div>

  <p class="muted text-[15px] mt-10 max-w-lg mx-auto leading-relaxed">
    Just say what you want, in plain words. Gaia builds the right specialist for it, every time, then puts it to work. No setup, no prompt wrangling, no fixed menu of bots to pick from.
  </p>
</section>

<section class="relative z-10 max-w-5xl mx-auto px-6 pb-10">
  <div class="grid grid-cols-2 md:grid-cols-4 gap-0 border border-[color:var(--rule)]">
    <div class="stat-block border-r border-[color:var(--rule)] p-6 text-center">
      <div class="display text-4xl font-bold" style="color:var(--moss);">∞</div>
      <div class="text-xs muted mt-2 tracking-wide uppercase" style="font-family:'Cinzel',serif;font-size:.65rem;letter-spacing:.2em;">Souls forged</div>
    </div>
    <div class="stat-block border-r border-[color:var(--rule)] p-6 text-center">
      <div class="display text-4xl font-bold" style="color:var(--moss);">200+</div>
      <div class="text-xs muted mt-2" style="font-family:'Cinzel',serif;font-size:.65rem;letter-spacing:.2em;text-transform:uppercase;">LLMs supported</div>
    </div>
    <div class="stat-block border-r border-[color:var(--rule)] p-6 text-center">
      <div class="display text-4xl font-bold" style="color:var(--moss);">MIT</div>
      <div class="text-xs muted mt-2" style="font-family:'Cinzel',serif;font-size:.65rem;letter-spacing:.2em;text-transform:uppercase;">License</div>
    </div>
    <div class="stat-block p-6 text-center">
      <div class="display text-4xl font-bold" style="color:var(--moss);">0</div>
      <div class="text-xs muted mt-2" style="font-family:'Cinzel',serif;font-size:.65rem;letter-spacing:.2em;text-transform:uppercase;">Walled gardens</div>
    </div>
  </div>
</section>

<section class="relative z-10 max-w-5xl mx-auto px-6 py-24">
  <div class="grid md:grid-cols-12 gap-12">
    <div class="md:col-span-5">
      <p class="eyebrow">II · The soul smith</p>
      <h2 class="display text-4xl md:text-5xl mt-4 leading-[1.05] tracking-tight">One agent.<br/>Many <span class="italic" style="color:var(--moss);">souls.</span></h2>
      <p class="muted text-[15px] mt-6 leading-relaxed">Gaia itself stays simple. The real trick is in how it hands things off.</p>
      <p class="muted text-[15px] mt-4 leading-relaxed">When something gets <span class="display italic" style="color:var(--ink);font-weight:600;">complex</span>, it passes the work to whichever agent fits best. That's how you get an expert on the job instead of a generalist guessing.</p>
      <p class="muted text-[15px] mt-4 leading-relaxed">And when nothing fits? Gaia just <span class="display italic" style="color:var(--ink);font-weight:600;">makes one</span>. Like the earth it's named after, it shapes a new soul with the right skills, the right model, the right temperament.</p>
      <blockquote class="dictum mt-8">cause why do the task<br/>when you can create a person<br/>that will do it for you?</blockquote>
    </div>
    <div class="md:col-span-7">
      <div class="smith">
        <p class="eyebrow mb-5" style="color:var(--gold);">Worked example</p>
        <div class="mono text-[12px] muted mb-4">"Help me build an A/B gym plan, then a webapp to track progress."</div>
        <div class="smith-grid">
          <div class="node"><span class="brand text-xs" style="color:var(--moss);">GAIA</span><span class="label">→ recognises a complex, two-part request</span></div>
          <div class="arrow">DELEGATES</div>
          <div class="node"><span class="role">soul · gym personal trainer</span><span class="label" style="margin-left:auto;">A/B routine</span></div>
          <div class="node"><span class="role">soul · senior web designer</span><span class="label" style="margin-left:auto;">progress webapp</span></div>
          <div class="arrow">RETURNS</div>
          <div class="node"><span class="brand text-xs" style="color:var(--moss);">GAIA</span><span class="label">→ stitches results, remembers what worked</span></div>
        </div>
      </div>
    </div>
  </div>
</section>

<hr class="hr-script max-w-5xl mx-auto" />

<section class="relative z-10 max-w-5xl mx-auto px-6 py-24">
  <div class="md:flex items-end justify-between gap-10 mb-14">
    <div>
      <p class="eyebrow">III · What it carries</p>
      <h2 class="display text-4xl md:text-5xl mt-4 leading-[1.05] tracking-tight max-w-xl">Five things every soul inherits.</h2>
    </div>
    <p class="muted text-[14px] max-w-sm mt-4 md:mt-0 leading-relaxed">Gaia and every agent it forges share the same foundation.</p>
  </div>
  <div class="grid md:grid-cols-2">
    <article class="card p-7 border-r-0 border-b-0">
      <svg class="ico" width="36" height="36" viewBox="0 0 32 32"><path d="M5 16h6l3-8 4 16 3-8h6"/><circle cx="11" cy="16" r="1.5"/><circle cx="21" cy="16" r="1.5"/></svg>
      <h3 class="display text-xl mt-5">Tools</h3>
      <p class="muted text-sm mt-3 leading-relaxed">Files, the browser, the network, a shell. Everything it needs to get from <span class="display italic" style="color:var(--ink);">planning</span> to <span class="display italic" style="color:var(--ink);">action</span> without you babysitting it.</p>
    </article>
    <article class="card p-7 border-b-0">
      <svg class="ico" width="36" height="36" viewBox="0 0 32 32"><path d="M16 4c0 6-6 8-6 14a6 6 0 0012 0c0-6-6-8-6-14zM10 18c2 0 4 1 6 3M22 18c-2 0-4 1-6 3"/></svg>
      <h3 class="display text-xl mt-5">Memory</h3>
      <p class="muted text-sm mt-3 leading-relaxed">Short term and long term, kind of like a person. The stuff that matters sticks around after the session ends, the noise doesn't, and it gets a little better at reading you every time you talk.</p>
    </article>
    <article class="card p-7 border-r-0 border-b-0">
      <svg class="ico" width="36" height="36" viewBox="0 0 32 32"><path d="M6 22c4-3 8-3 12 0s8 3 12 0M6 14c4-3 8-3 12 0s8 3 12 0"/><circle cx="16" cy="6" r="2"/></svg>
      <h3 class="display text-xl mt-5">Self-learning loop</h3>
      <p class="muted text-sm mt-3 leading-relaxed">It doesn't make the same mistake twice. Between runs, Gaia and its souls quietly rewrite their own instructions, swap in better tools, and tune themselves to how you like things done.</p>
    </article>
    <article class="card p-7 border-b-0">
      <svg class="ico" width="36" height="36" viewBox="0 0 32 32"><rect x="4" y="6" width="24" height="20"/><path d="M4 12h24M9 19h6M9 22h10"/><circle cx="22" cy="20" r="2.5"/></svg>
      <h3 class="display text-xl mt-5">Skills <span class="muted" style="font-weight:400;font-style:italic;font-size:.82em;">+ MCP</span></h3>
      <p class="muted text-sm mt-3 leading-relaxed">Hundreds of MCP servers, plus skills that are just plain text files you can read, share, and mix together. Want to give it a new ability? Drop in a file. You always see exactly what your agent sees.</p>
    </article>
    <article class="card p-7 md:col-span-2">
      <div class="md:flex items-start gap-8">
        <svg class="ico shrink-0" width="42" height="42" viewBox="0 0 32 32"><circle cx="16" cy="16" r="11"/><path d="M5 16h22M16 5c4 4 4 18 0 22M16 5c-4 4-4 18 0 22"/><circle cx="16" cy="16" r="2.5" stroke="none" style="fill:var(--moss);"/></svg>
        <div>
          <h3 class="display text-xl mt-1">Connectors</h3>
          <p class="muted text-sm mt-3 leading-relaxed max-w-2xl">Hook it up to WhatsApp, Telegram, Discord, whatever you already use. It reads your messages, answers in your voice when you want it to, and pings you when something actually needs you.</p>
        </div>
      </div>
    </article>
  </div>
</section>

<hr class="hr-script max-w-5xl mx-auto" />

<section class="relative z-10 max-w-5xl mx-auto px-6 py-24">
  <div class="text-center mb-14">
    <p class="eyebrow">IV · How it works</p>
    <h2 class="display text-4xl md:text-5xl mt-4 leading-[1.05] tracking-tight">Three steps. Then it runs.</h2>
  </div>
  <div class="grid md:grid-cols-3 gap-0 relative">
    <div class="how-step p-8 border border-r-0 border-[color:var(--rule)] text-center">
      <div class="how-num">01</div>
      <h3 class="display text-xl mt-4 font-semibold">Plant a goal</h3>
      <p class="muted text-sm mt-3 leading-relaxed">Just tell Gaia what you want, in plain words, on whatever app you already text on. No prompt engineering, no special syntax.</p>
    </div>
    <div class="how-step p-8 border border-r-0 border-[color:var(--rule)] text-center how-active">
      <div class="how-num" style="color:var(--moss);border-color:var(--moss);">02</div>
      <h3 class="display text-xl mt-4 font-semibold">Gaia delegates</h3>
      <p class="muted text-sm mt-3 leading-relaxed">It pulls the task apart and hands each piece to the soul that fits best, or builds a new one right there if it has to.</p>
    </div>
    <div class="how-step p-8 border border-[color:var(--rule)] text-center">
      <div class="how-num">03</div>
      <h3 class="display text-xl mt-4 font-semibold">Watch it grow</h3>
      <p class="muted text-sm mt-3 leading-relaxed">Everything comes back put together in one place. Gaia keeps what worked, so the next job starts a step ahead.</p>
    </div>
  </div>
</section>

<hr class="hr-script max-w-5xl mx-auto" />

<section class="relative z-10 max-w-5xl mx-auto px-6 py-24">
  <div class="grid md:grid-cols-12 gap-12">
    <div class="md:col-span-4">
      <p class="eyebrow">V · FAQ</p>
      <h2 class="display text-3xl md:text-4xl mt-4 leading-tight tracking-tight">Common questions.</h2>
      <p class="muted text-[14px] mt-4 leading-relaxed">The stuff people usually want to know first.</p>
    </div>
    <div class="md:col-span-8">
      <details class="faq-item">
        <summary class="faq-q">
          <span>Is Gaia really free?</span><span class="faq-icon" aria-hidden="true">+</span>
        </summary>
        <div class="faq-a">Yep. It's MIT licensed with no usage fees. You bring your own API keys (OpenAI, Anthropic, OpenRouter, whatever you've got) or just point it at a local model. Gaia itself costs you nothing.</div>
      </details>
      <details class="faq-item">
        <summary class="faq-q">
          <span>What LLMs does it support?</span><span class="faq-icon" aria-hidden="true">+</span>
        </summary>
        <div class="faq-a">Pretty much anything with an API. OpenAI, Anthropic, Google Gemini, Mistral, and 200+ more through OpenRouter. You can even give each soul its own model: something cheap and fast for the easy stuff, something heavier when it actually matters.</div>
      </details>
      <details class="faq-item">
        <summary class="faq-q">
          <span>How is Gaia different from AutoGPT or CrewAI?</span><span class="faq-icon" aria-hidden="true">+</span>
        </summary>
        <div class="faq-a">With AutoGPT or CrewAI you have to define your agents up front. Gaia's soul smith makes the specialists it needs as it goes, so you don't have to know in advance what you'll need. The whole setup sort of builds itself around the work.</div>
      </details>
      <details class="faq-item">
        <summary class="faq-q">
          <span>Does my data stay private?</span><span class="faq-icon" aria-hidden="true">+</span>
        </summary>
        <div class="faq-a">It does. Gaia runs on your own machine or your own server, and its memory stays right there with it. Nothing gets sent back to us. The only thing that ever leaves your box is the call to whatever LLM you set up.</div>
      </details>
      <details class="faq-item">
        <summary class="faq-q">
          <span>Can I connect it to WhatsApp or Telegram?</span><span class="faq-icon" aria-hidden="true">+</span>
        </summary>
        <div class="faq-a">Yeah, WhatsApp, Telegram, and Discord connectors come built in. You can text Gaia from your phone while it does the actual work back on your server.</div>
      </details>
    </div>
  </div>
</section>

<hr class="hr-script max-w-5xl mx-auto" />

<section id="voices" class="relative z-10 max-w-5xl mx-auto px-6 py-24">
  <div class="text-center mb-14">
    <p class="eyebrow">VI · Voices</p>
    <h2 class="display text-4xl md:text-5xl mt-4 leading-[1.05] tracking-tight">What people are saying.</h2>
    <p class="muted text-[14px] mt-4 leading-relaxed max-w-xl mx-auto">A few early users who handed Gaia something messy and watched it sort itself out.</p>
  </div>

  <!-- TODO(itay): replace with real testimonials. Placeholders below. -->
  <div class="grid md:grid-cols-3 gap-0">
    <figure class="quote-card border-r-0 md:border-r-0 border-b-0 md:border-b">
      <span class="mark" aria-hidden="true">&ldquo;</span>
      <blockquote>I asked for a trip plan and it spun up a travel agent that booked everything. Felt like hiring someone, not prompting a bot.</blockquote>
      <figcaption class="who">
        <span class="avatar" aria-hidden="true">MR</span>
        <span><span class="name">Maya R.</span><br/><span class="handle">@mayabuilds</span></span>
      </figcaption>
    </figure>
    <figure class="quote-card border-r-0 border-b-0">
      <span class="mark" aria-hidden="true">&ldquo;</span>
      <blockquote>The soul smith is wild. No setup, no crew to define ahead of time. It just made the specialist it needed and got to work.</blockquote>
      <figcaption class="who">
        <span class="avatar" aria-hidden="true">DK</span>
        <span><span class="name">Deniz K.</span><br/><span class="handle">@denizdev</span></span>
      </figcaption>
    </figure>
    <figure class="quote-card border-b-0">
      <span class="mark" aria-hidden="true">&ldquo;</span>
      <blockquote>Self-hosted it on a $5 VPS, wired it to Telegram, and now it triages my inbox before I wake up. MIT and free is unreal.</blockquote>
      <figcaption class="who">
        <span class="avatar" aria-hidden="true">JL</span>
        <span><span class="name">Jonas L.</span><br/><span class="handle">@jonaslin</span></span>
      </figcaption>
    </figure>
  </div>

  <!-- Tweet wall: dormant. Wire later to X embed / API, render cards into #tweetWall. -->
  <div id="tweetWall" class="grid md:grid-cols-3 gap-0 mt-0"></div>

  <div class="text-center mt-12">
    <a id="shareX" href="#" target="_blank" rel="noopener" class="btn btn-ghost">Share on X →</a>
    <p class="muted text-xs mt-3 mono">Tag <span style="color:var(--moss);" id="tagLabel">#gaiaagent</span> · we feature the best.</p>
  </div>
</section>

<hr class="hr-script max-w-5xl mx-auto" />

<section id="contact" class="relative z-10 max-w-5xl mx-auto px-6 py-28">
  <div class="grid md:grid-cols-12 gap-10 items-end">
    <div class="md:col-span-7">
      <p class="eyebrow">VII · Get in touch</p>
      <h2 class="display text-4xl md:text-5xl mt-4 leading-[1.05] tracking-tight">Plant a goal. <span class="italic" style="color:var(--moss);">See what grows.</span></h2>
      <p class="muted text-[15px] mt-5 max-w-md leading-relaxed">It's free and open source. Star the repo, or come say hi on GitHub.</p>
    </div>
    <div class="md:col-span-5 flex flex-col gap-3">
      <a href="https://github.com/sho0pi" target="_blank" class="btn btn-primary justify-between">github.com/sho0pi <span aria-hidden="true">↗</span></a>
      <a href="https://github.com/sho0pi/gaia" target="_blank" class="btn btn-ghost justify-between">Star the repo <span aria-hidden="true">↗</span></a>
    </div>
  </div>
</section>

<script is:inline>
  function copyInstall() {
    var cmd = document.getElementById('installCmd').textContent;
    var btn = document.getElementById('copyBtn');
    navigator.clipboard.writeText(cmd).then(function() {
      btn.textContent = 'copied!';
      btn.classList.add('copied');
      setTimeout(function() { btn.textContent = 'copy'; btn.classList.remove('copied'); }, 2000);
    });
  }

  // GitHub star count
  fetch('https://api.github.com/repos/sho0pi/gaia')
    .then(function(r) { return r.json(); })
    .then(function(d) {
      var el = document.getElementById('starCount');
      if (el && d.stargazers_count !== undefined) {
        var n = d.stargazers_count;
        el.textContent = n >= 1000 ? (n/1000).toFixed(1) + 'k' : n;
      }
    })
    .catch(function() {}); // repo not public yet, silently fail

  // === Social / tweet wall ===
  // Single source of truth for the campaign hashtag. Change here to rebrand.
  var SOCIAL_TAG = 'gaiaagent';

  (function () {
    var label = document.getElementById('tagLabel');
    if (label) label.textContent = '#' + SOCIAL_TAG;
    var share = document.getElementById('shareX');
    if (share) {
      var text = 'Just let @gaia forge an agent for me. #' + SOCIAL_TAG;
      share.href = 'https://twitter.com/intent/tweet?text=' +
        encodeURIComponent(text) + '&url=' + encodeURIComponent('https://gaia-agent.com');
    }
  })();

  // Tweet wall — dormant. When ready, fetch tweets tagged #<SOCIAL_TAG> (X API or
  // a cached JSON you generate server-side) and pass them here. Stays hidden while empty.
  function renderTweetWall(tweets) {
    var wall = document.getElementById('tweetWall');
    if (!wall || !tweets || !tweets.length) return;
    wall.classList.add('mt-12');
    wall.innerHTML = tweets.map(function (t) {
      return '<figure class="quote-card border-r-0 border-b-0">' +
        '<span class="mark" aria-hidden="true">&ldquo;</span>' +
        '<blockquote>' + t.text + '</blockquote>' +
        '<figcaption class="who"><span class="avatar" aria-hidden="true">' +
        (t.name ? t.name.charAt(0) : '@') + '</span><span><span class="name">' +
        (t.name || '') + '</span><br/><span class="handle">@' + t.handle + '</span></span></figcaption>' +
        '</figure>';
    }).join('');
  }
  // renderTweetWall(await fetch('/tweets.json').then(r => r.json()));  // ← wire later

  // Forge demo — cycle real tasks into best-fit agent profiles.
  (function () {
    var reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    var examples = [
      {
        request: 'Plan a two-week Japan trip around food, trains, and quiet hotels.',
        meta: 'needs: research · itinerary · booking logic',
        role: 'soul · travel operations planner',
        fit: 'Best fit for routes, constraints, reservations, budgets, and day-by-day sequencing.',
        tools: ['browser', 'maps', 'calendar']
      },
      {
        request: 'Turn my messy gym notes into an A/B plan I can follow every week.',
        meta: 'needs: coaching · structure · tracking',
        role: 'soul · strength program coach',
        fit: 'Best fit for progressive overload, recovery windows, exercise swaps, and measurable routines.',
        tools: ['memory', 'spreadsheet', 'habit loop']
      },
      {
        request: 'Build me a launch page and make it feel alive.',
        meta: 'needs: copy · design · frontend',
        role: 'soul · product web designer',
        fit: 'Best fit for turning rough intent into a polished page, copy, layout, and deployable frontend.',
        tools: ['Astro', 'Tailwind', 'copy pass']
      },
      {
        request: 'Clean my inbox and only tell me what actually needs me.',
        meta: 'needs: triage · tone · prioritization',
        role: 'soul · executive inbox operator',
        fit: 'Best fit for sorting threads, drafting replies, flagging decisions, and preserving your voice.',
        tools: ['email', 'rules', 'summaries']
      }
    ];
    var requestCard = document.getElementById('requestCard');
    var agentCard = document.getElementById('agentCard');
    var requestText = document.getElementById('requestText');
    var requestMeta = document.getElementById('requestMeta');
    var agentRole = document.getElementById('agentRole');
    var agentFit = document.getElementById('agentFit');
    var agentTools = document.getElementById('agentTools');
    var steps = Array.prototype.slice.call(document.querySelectorAll('[data-forge-step]'));
    if (!requestCard || !agentCard || !requestText || !agentRole || reduce) return;
    var i = 0;
    function setStep(active) {
      steps.forEach(function (step, idx) { step.classList.toggle('active', idx <= active); });
    }
    function render(example) {
      requestText.textContent = example.request;
      requestMeta.textContent = example.meta;
      agentRole.textContent = example.role;
      agentFit.textContent = example.fit;
      agentTools.innerHTML = '';
      example.tools.forEach(function (tool) {
        var tag = document.createElement('span');
        tag.textContent = tool;
        agentTools.appendChild(tag);
      });
    }
    function cycleForge() {
      i = (i + 1) % examples.length;
      requestCard.classList.add('is-switching');
      agentCard.classList.add('is-switching');
      setStep(0);
      setTimeout(function () { setStep(1); }, 360);
      setTimeout(function () {
        render(examples[i]);
        setStep(2);
        requestCard.classList.remove('is-switching');
        agentCard.classList.remove('is-switching');
      }, 720);
    }
    setInterval(cycleForge, 15000);
  })();

  // Verb cycle — no reduced-motion guard, always runs
  var verbs = ['forge', 'delegate', 'teach', 'build'];
  var vi = 0;
  var verbEl = document.getElementById('verb');
  setInterval(function() {
    verbEl.classList.add('out');
    setTimeout(function() {
      vi = (vi + 1) % verbs.length;
      verbEl.textContent = verbs[vi];
      verbEl.classList.remove('out');
      verbEl.classList.add('in');
      requestAnimationFrame(function() {
        requestAnimationFrame(function() {
          verbEl.classList.remove('in');
        });
      });
    }, 400);
  }, 3500);
</script>

</Layout>
